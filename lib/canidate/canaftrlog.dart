
import 'package:flutter/material.dart';

class CanafterLog extends StatelessWidget {
  const CanafterLog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Container(),
    );
  }
}
